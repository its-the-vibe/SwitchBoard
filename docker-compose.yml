version: '3.8'

services:
  switchboard:
    build: .
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
    volumes:
      - ./config.json:/root/config.json:ro
    restart: unless-stopped
